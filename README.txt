# Setup Instructions

1. Install the required Python packages:
   pip install flask flask-wtf wtforms flask-mysqldb mysqlclient bootstrap-flask

2. Import the provided database:
   mysql -u root -p < project/database.sql

3. Run the application:
   python run.py
